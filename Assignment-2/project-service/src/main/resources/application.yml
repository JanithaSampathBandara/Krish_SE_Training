spring:
  datasource:
    url: jdbc:mysql://localhost:3306/projectservice?createDatabaseIfNotExist=true
    username: root
    password:
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
  application:
    name: project
server:
  port: 8082
